'use client';

export default function ChangePasswordButton() {
  const handleChangePassword = () => {
    // TODO:
  };

  return (
    <button
      className="rounded-md w-full px-4 py-2 border bg-white"
      onClick={handleChangePassword}
    >
      Change password
    </button>
  );
}
